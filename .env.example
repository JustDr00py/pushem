# Server Configuration
PORT=8080

# Directory containing the frontend build artifacts
# If running via Docker Compose (as configured), this is set automatically.
# If running locally, this should be relative to the execution directory.
STATIC_DIR=web/dist

# CORS Configuration
# IMPORTANT: Configure this for production to restrict API access
# Format: Comma-separated list of allowed origins
# Examples:
#   Single domain:  CORS_ORIGINS=https://your-domain.com
#   Multiple:       CORS_ORIGINS=https://domain1.com,https://domain2.com
#   With wildcard:  CORS_ORIGINS=https://*.yourdomain.com
#   Development:    CORS_ORIGINS=http://localhost:*,https://localhost:*
#
# Default (if not set): http://localhost:*,https://localhost:* (secure default)
# For public API (allows any origin): CORS_ORIGINS=https://*,http://*
CORS_ORIGINS=https://your-domain.com

# VAPID Configuration
# The email address used as the "sub" claim in the VAPID JWT.
# This must be a valid mailto: link (e.g., mailto:admin@example.com).
# CRITICAL: Set this to a real email address to ensure delivery to iOS devices
# and avoid 403 Forbidden errors from Apple's Push Notification server.
VAPID_SUBJECT=mailto:admin@yourdomain.com

# Message History Configuration
# Number of days to keep message history before automatic cleanup
# Default: 7 days
MESSAGE_RETENTION_DAYS=7

# Cleanup interval in hours
# Default: 24 hours (once per day)
CLEANUP_INTERVAL_HOURS=24
